# Normalize line endings to LF in the repo, checkout as-is on Windows
* text=auto eol=lf

# Always LF for shell scripts
*.sh text eol=lf

# Treat these as binary to avoid mangling
*.png binary
*.jpg binary
*.ico binary
